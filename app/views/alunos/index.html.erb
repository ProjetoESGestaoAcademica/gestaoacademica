<p align="right"><%= link_to 'Logout', logout_url %></p>
<h1>Alunos</h1>

<%= link_to 'Cadastrar aluno', new_aluno_path %>

<br>
<br>
<br>

<table>
  <tr>
    <th>Nome</th>
    <th>Idade</th>
    <th></th>
  </tr>

 <% @alunos.each do |aluno| %>
    <tr>
      <td><%= aluno.nome %></td>
      <td><%= aluno.idade %></td>
      <td><%= link_to 'Mostrar', aluno_path(aluno) %></td>
      <td><%= link_to 'Editar', edit_aluno_path(aluno) %></td>
      <td><%= link_to 'Destruir', aluno_path(aluno),
                      method: :delete,
                      data: { confirm: 'Você esta seguro disto?' } %></td>
    </tr>
  <% end %>
</table>

<br>
<br>

<%= link_to 'Back', root_url %>